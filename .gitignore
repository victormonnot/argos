# --- Privé : ne jamais publier (stratégie de sprint personnelle) ---
argos-plan-sprint.md
argos-plan-swarm.md
IDEES_V2.md
PORTFOLIO.md

# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
venv-ardupilot/

# --- Artefacts SITL / ArduPilot (logs de vol, télémétrie, params) ---
*.tlog
*.tlog.raw
*.BIN
eeprom.bin
mav.parm
logs/

# --- Modèles / poids lourds + sorties d'entraînement (perception, S2+) ---
*.engine
*.onnx
*.pt
*.weights
# sorties Ultralytics (results.csv, plots) + weights_dir (poids téléchargés)
runs/
weights/

# --- Dialecte MAVLink : la source est argos.xml, le reste se régénère ---
# Du code généré committé, ce sont deux sources de vérité qui divergent en
# silence le jour où l'une des deux est modifiée à la main.
mavlink/.build/
mavlink/generated/

# --- OS / éditeurs ---
.DS_Store
.vscode/
*.swp



settings.json
mavlink/c_demo/argos_listen

