# ARGOS perception — détection temps réel sur edge.
# (tensorrt est ajouté à l'étape export/benchmark : pip install tensorrt)
ultralytics>=8.3
onnx
# Mode A — console opérateur web
fastapi
uvicorn[standard]
